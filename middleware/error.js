/*

const errorHandler = (err, req, res, next) => {    // Global error handler (optional)
    if(err.status) {
        res.status(err.status).json( { msg: err.message});
    }
    else
       res.status(500).json( { msg: err.message})

}

*/
